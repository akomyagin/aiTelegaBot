# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
/bin/
/dist/
go.work
go.work.sum

# Local state / data (digest history, subscriptions) — never commit
*.db
*.db-journal
*.sqlite
*.sqlite3

# MTProto session (if applicable) — extremely sensitive, full account access
*.session
*.session-journal

# Env / secrets
.env
*.local.yaml

# Node (if a TS sidecar is used)
node_modules/
dist/

# IDE
.idea/
.vscode/
